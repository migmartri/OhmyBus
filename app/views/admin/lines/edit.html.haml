.span-23.form
  .box.new-line
    - form_for @line, :url => admin_line_path, :html => {:onsubmit => "encodePolyline()"} do |f|
      = render :partial => 'form', :locals => {:f => f}
      .span-24.mar-5
        = f.submit "Guardar"
        »
        = link_to "Volver", admin_lines_path
    .clear


- content_for :legend do
  %p
    Desde este panel puedes editar una linea ya existente, crear nuevos puntos por los que pasa, moverlos de un punto a otro del mapa o eliminarlos haciendo click sobre ellos.

%script
  = "window.onload = initMapNewLine();loadCourse('#{@line.escaped_course}');"